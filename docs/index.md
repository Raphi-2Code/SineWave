# ursina_webview

Browser WebView integration for Ursina using `cefpython3` with an optional PyQt6 fallback.

**Note**: `cefpython3` relies on an outdated Chromium engine. For latest features you may use the Qt fallback if available.
